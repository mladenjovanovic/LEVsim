% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create-sets.R
\name{create_sets}
\alias{create_sets}
\title{Create Sets}
\usage{
create_sets(
  LEV_profile,
  load,
  load_type = "absolute",
  max_reps = 100,
  failed_reps = FALSE,
  L0_fatigue = 0,
  V0_fatigue = 0
)
}
\arguments{
\item{LEV_profile}{\code{LEV_profile} object, returned by \code{\link{create_visits}} function}

\item{load}{Numeric vector. Loads are either absolute weight or percentages. See also \code{load_type}}

\item{load_type}{Type of load calculation. Can be either 'absolute' (default), or 'visit 1RM', or
'prescription 1RM'}

\item{max_reps}{How many maximum reps to generate? Default is 100}

\item{failed_reps}{Should failed-reps be included in the output? Default is \code{FALSE}}

\item{L0_fatigue}{Multiplicative decrease in L0 across sets in percentage. Default is 0}

\item{V0_fatigue}{Multiplicative decrease in V0 across sets in percentage. Default is 0}
}
\value{
Object \code{LEV_sets}
}
\description{
Function creates sets, using provided \code{LEV_profile} \code{loads} and \code{trials}
}
\examples{
# Create random athletes/profiles
set.seed(1667)

sets <- create_athletes(5) \%>\%
  create_visits(1:3) \%>\%
  create_sets(
    load = c(90, 110, 130),
    load_type = "absolute"
  )

print(sets)

# Extract data frame
LEV_data <- as.data.frame(sets)
# Or
# LEV_data <- coef(sets)

plot(sets)
plot(sets, athletes = "Athlete 1")
plot(sets, athletes = "Athlete 1", reps = 1)
plot(sets, athletes = "Athlete 1", x_var = "RIR")

# Another way to create LEV profiles
sets <- create_profiles(athletes = c("Mladen", "Ivan"), L0 = c(200, 180)) \%>\%
  create_visits(1) \%>\%
  create_sets(load = c(100, 120, 140), load_type = "absolute")

plot(sets)
plot(sets, facet = NULL, x_var = "load")
plot(sets, visits = 1, x_var = "RIR")
}
